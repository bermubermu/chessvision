<%= form_for @player do |f| %>
  <%= render 'shared/error_messages', :object => f.object %>
  <div class="field7">
		<% if cookies[:langu]== "en" %>
			<%= f.submit "Submit Request >>", :class => "redtextoo" %>
		<% else %>
			<%= f.submit "Inscribirse >>", :class => "redtextoo" %>
		<% end %>
  </div>
<% end %>





